# GradeOps – Python dependencies

# Core
pydantic>=2.0
python-dotenv

# PDF processing
pymupdf>=1.23          # fitz – PDF to image rasterisation
Pillow>=10.0

# VLM Extraction
nougat-ocr             # facebook/nougat-base  (LaTeX-heavy documents)
transformers>=4.40     # Qwen-VL               (handwritten text)
accelerate             # multi-GPU / fast load for transformers
torch>=2.1

# Agentic evaluation
langchain>=0.2
langchain-openai>=0.1
langgraph>=0.1
tenacity>=8.2

# API / Dashboard
fastapi>=0.110
uvicorn[standard]
httpx

# Testing
pytest
pytest-asyncio
