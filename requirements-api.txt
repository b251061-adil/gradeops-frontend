# GradeOps API – Lightweight Dependencies (Vercel Deployment)
# Excludes heavy ML packages (torch, transformers, nougat-ocr)
# Use requirements.txt for full pipeline with ML support

# Core
pydantic>=2.0
python-dotenv

# API / Dashboard
fastapi>=0.110
uvicorn[standard]
httpx
